<% layout("./layouts/boilerplate.ejs") %>

<div class="mt-10 flex flex-row justify-center items-center">
    <% if(currentUser) {%>
    <p class="text-xl sm:text-2xl">Welcome Admin Panel</p>

        <% } else { %>
    <div class="flex flex-col">
        <p class="text-xl sm:text-2xl">If you are new then first <a href="/account/signup" class="text-blue-500">signup</a></p>
        <br>
        <p class="text-xl sm:text-2xl mt-5">Already have account <a href="/account/login" class="text-blue-500">login</a></p>
    </div>

        <% } %>
</div>